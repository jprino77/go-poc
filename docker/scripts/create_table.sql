CREATE TABLE pokedex (
                         id NUMBER PRIMARY KEY,
                         pokemon_id NUMBER,
                         type VARCHAR2(50),
                         alias VARCHAR2(100)
);
